<h1>接続先<%=h '(サーバ設置場所)' if controller.admin_user? %>の編集</h1>

<% form_for(@location) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => f %>
  <p>
    <%= f.submit '更新' %>
  </p>
<% end %>

<%= link_to '詳細', @location %> |
<%= link_to '戻る', locations_path %>
