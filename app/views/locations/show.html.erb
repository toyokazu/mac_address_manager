<p>
  <b>接続先:</b>
  <%=h @location.description %>
</p>

<p>
  <b>ネットワーク機器:</b><br/>
  <p>
    ホスト名: <%=h @location.hostname %>
  </p>
<% if controller.admin_user? %>
  <p>
    ホストの種類: <%=h @location.hosttype %>
  </p>
  <p>
    IPv4 アドレス: <%=h @location.ipv4_addr %>
  </p>
  <p>
    IPv6 アドレス: <%=h @location.ipv6_addr %>
  </p>
<% end %>
</p>
<% if controller.admin_user? %>
<p>
  <b>接続先候補:</b>
  <%=h @location.selectable ? '含める' : '含めない' %>
</p>
<% end %>

<%= link_to '編集', edit_location_path(@location) %> |
<%= link_to '戻る', locations_path %>
